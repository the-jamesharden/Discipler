# Reference Material

These files are historical product references used to inform the clean Discipler definition.

They should not be silently edited to match newer decisions.

If a newer product decision conflicts with a reference document, record the newer decision in the canonical product docs and allow `/grill-with-docs` to surface and resolve the conflict explicitly.
